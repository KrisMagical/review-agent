# MagicReview Internal App Package

This package contains the current internal review implementation used by the public `magicreview` package and `mgreview` CLI.

The public user-facing package name is `magicreview`; this `app` package remains internal until the implementation is fully folded into the new package structure.
